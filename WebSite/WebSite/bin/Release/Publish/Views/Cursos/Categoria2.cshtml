@model IEnumerable<CadastroLogin.Models.Cursos>
@{ 
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <!--------------------------BANNER-SECTION---------------------------->

    <section id="banner" class="bg-purple">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="promo-title" style="color: #FFF;">@CadastroLogin.Resources.GlobalResource.FrontEnd</p>
                    <p style="color: #FFF;">
                        Breve descrição sobre nossos serviços, sobre o que fazemos, cursos e etc, não sei mais o que escrever e só estou escrevendo pra encher linguiça
                        , porque estou com preguiça de ir pegar no Lorem Ipsum.
                    </p>
                </div>
                <div class="col-md-6 text-center">
                    <img src="~/image/grafico.png" class="img-fluid">
                </div>
            </div>
        </div>
    </section>

    <!--------------------------COURSES-SECTION---------------------------->
    <div class="col-lg-12 mb-5 text-center">
        <h2><span class="color-purple"> Lista de cursos</span></h2>
    </div>
    @foreach (var item in Model)
    {
        <section id="section-cursos">
            <div class="container">
                <div class="row mt-5">
                    <div class="col-md-">
                        <div class="card-deck">
                            <div class="card">
                                <a href="@Url.Action("Detalhes", "Cursos", new { id=item.Id_Curso })" title="List View" class="links">
                                    <img alt="List View" src="@Url.Content("~/image/grafico.png")">
                                </a>
                                <div class="card-body">
                                    <h5 class="card-title">@Html.DisplayFor(modelItem => item.Nome)</h5>
                                    <p class="card-text">@Html.DisplayFor(modelItem => item.Desc_cur)</p>
                                </div>
                                <div class="card-footer">
                                    <small class="text-muted">Atualizados 2 dias atrás</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    }
    <script src="~/js/bootstrap.min.js"></script>

@*</body>*@
@*</html>*@